# Selection dictionary

The dictionary can be enabled independently from message translation. Selecting up to 120 characters in a Discord message reveals an `Aa` action. The same selection path applies to text rendered inside link previews and social-post cards, even before the translation scanner assigns an internal root identifier to the card. Lookup starts only after the user activates it. Results may include a headword, reading, part of speech, definition, example, source, license, and operating-system speech synthesis.

Installed packs and personal terms stay on the device in a dedicated `dictionary.db` SQLite file. Nearby message context is used only on the device and is never included in translation or external-dictionary requests. When the configured translation model is ready, the app may send the selected phrase and pack definitions that are unavailable in the interface language to that model. If the configured model is external, those texts leave the device; this transfer is disclosed in settings. Choosing the optional Wiktionary action also sends the selected phrase to the external site. Reference translations never replace source-authored dictionary rows.

## Definition locale and fallback order

The interface language, not the message display-translation language, selects the preferred definition locale. This means a Korean interface requests Korean definitions, a Japanese interface requests Japanese definitions, and a Chinese interface requests Chinese definitions regardless of the current Discord translation direction.

Lookup uses the following order for each entry:

1. A human-authored definition in the interface language from the installed pack.
2. A previously cached automatic translation that passed the current structural quality gate for that entry and interface language.
3. The pack's English definition, or its first available original definition.

When only step 3 is available and the configured translation model is ready, the local rows are displayed immediately while localization continues in the existing translation worker. The full selection and the first visible sense of each distinct headword are sent as one batch instead of serial requests; collapsed alternative senses remain in their source language unless a later lookup promotes one to the visible sense. A thin placeholder is shown only beneath the popup title while the full-selection meaning is pending, and the localized update preserves the user's scroll position. Superseded pending dictionary jobs are removed from the queue when a newer lookup arrives.

A translated gloss is accepted only when its length is bounded, it uses the expected writing system for the interface language, and it does not contain common model-answer wrappers or control markers. Accepted results are stored as a versioned localized overlay in `dictionary_localized_text`, while the source dictionary rows remain unchanged. Overlays created before the current quality version are ignored and regenerated on demand. The popup labels the result as a reference translation and expands the source dictionary definition by default. If translation is unavailable, rejected, or fails, the popup displays the original definition with its language instead of failing the lookup.

This gate catches malformed or clearly wrong-language output; it is not a semantic proof and is not presented as human review. Human-authored interface-language glosses from a pack remain the trusted first choice. Automatic localization is always secondary evidence alongside the immutable source gloss.

## Rough selection and phrase segmentation

Users do not need to know the exact dictionary boundary before selecting text. The app first attempts an exact lookup. When that misses, it checks shorter expressions inside the selection and returns a left-to-right, longest-match segmentation with up to eight distinct terms. Space-delimited languages keep word boundaries, while Korean, Japanese, Chinese, and Thai also support compact expressions without spaces. For example, a rough selection of `非難禁止` can resolve to separate `非難` and `禁止` entries when the combined expression is not a headword.

Before a segmented candidate is accepted, every one of the 28 product languages selects a declared analysis profile for word or compact-script boundaries and its available inflection strategy. Korean additionally builds a coverage lattice that distinguishes dictionary headwords from non-display grammatical endings. Attached forms such as `거지` in `많다는거지` and `이지` in `무슨조건이지` can complete the selected token without surfacing an unrelated noun sense, while exact or space-delimited nouns remain available. An internal inflection is returned only when dictionary and grammar spans cover the token from its real beginning, preventing `탐내요` from exposing the unrelated `내다` while allowing a sequence such as `자주해주냐` to resolve verified base forms. Common casual endings including `네`, `군요`, `구나`, `냐`, and double past forms are normalized before lookup. Bounded aliases restore conventional colloquial forms such as `왤케` to `왜 이렇게` and recognize `거같` and `것같` boundaries without changing the displayed selection. Every generated candidate must still exist in an installed pack; the analyser never generates a meaning.

If an exact lookup misses, the installed expanded languages can try a bounded local base-form fallback before phrase segmentation. Japanese covers common godan, ichidan, irregular, polite, negative, continuative, past, and adjective endings; Korean covers common particles including approximation particles, regular speech endings, productive `하다` contractions, and a small reviewed irregular set; English covers common plural, progressive, past, and high-frequency irregular forms. For example, `巡り`, `食べた`, `遊んで`, `먹어요`, `했어요`, `맹해`, `점심쯤`, `experiences`, and `running` can resolve to `巡る`, `食べる`, `遊ぶ`, `먹다`, `하다`, `맹하다`, `점심`, `experience`, and `run`. Exact dictionary and personal entries normally win. English morphology is explicit enough that an existing surface entry such as the adjective `completed` can be kept alongside the base verb `complete`. When Japanese context immediately continues an exact noun-shaped surface with a grammatical suffix such as `つつ`, `ながら`, or `ます`, the matching base-form entry is also returned ahead of the exact surface entry. Generated candidates remain bounded and are shown only when they actually exist in an installed pack. Chinese does not use inflection fallback; its Simplified and Traditional packs keep separate script-correct headwords.

The popup keeps the original selection as its title and, when the source and interface languages differ, places a translation of the full selection directly beneath it. It does not add a separate sentence-analysis or grammar section. Segmented results remain dictionary headwords with source-authored meanings, and each matched headword and reading keeps its own heading. The header can speak the full selection, while every segmented headword has its own operating-system speech-synthesis control using that entry's source language.

Source-language detection prefers a reliable signal in the selected text itself. For ambiguous Han-only selections, up to 240 characters immediately around the selection are used locally so Japanese or Chinese packs can still be chosen from nearby context without an unrelated language elsewhere in a multilingual message overriding the result. During segmentation, installed base-form candidates are also checked for each bounded surface span, so a polite phrase such as `お世話になります` can resolve to `お世話になる` instead of unrelated shorter headwords. Nearby source context remains local. The accepted full-selection translation is used only to rerank matching senses after dictionary definitions are localized; it does not generate headwords or overwrite dictionary meanings.

When a matched headword has several distinct senses, the lookup keeps every source-authored definition instead of collapsing the headword to its first sense. Default order is established while building the pack: source-linked translation and example counts provide bounded popularity evidence, while explicitly marked historical, archaic, dated, and figurative senses receive conservative priors. These signals affect only `senseRank`; source definitions and examples are never rewritten or copied into the pack as generated content.

At lookup time, the surrounding selection context is scored locally against those immutable definitions. Definition length is not a ranking signal. Lexical overlap and explicit domain cues may reorder senses, but a **Shown first for context** label appears only when the leading sense crosses both an evidence threshold and a score-margin threshold. Otherwise the pack's safe default order is preserved without implying that context was understood. Historical, archaic, dated, and figurative senses remain available under **Other meanings** and can be promoted when the selection contains matching cues. The ranker never generates or rewrites a definition, so a ranking error cannot replace the dictionary source text with a model-created meaning.

## Pack policy

The app keeps a four-entry project-authored mini pack for each core language so a first lookup can explain the feature without a setup step. Five user-facing expanded packs are bundled as gzip resources and expanded into SQLite only when the user selects **Install**. The internal `practical` edition marker remains a storage-compatibility detail and is not a product tier:

| Language | Headwords | Meaning entries | Compressed | Definitions |
|---|---:|---:|---:|---|
| Korean | 106,253 | 153,321 | 27,402,878 bytes | Korean + 10 localized definition languages |
| English | 127,311 | 182,055 | 5,241,208 bytes | English |
| Japanese | 464,728 | 584,460 | 10,220,505 bytes | English |
| Simplified Chinese | 121,119 | 124,750 | 3,994,942 bytes | English |
| Traditional Chinese | 122,302 | 124,877 | 3,989,983 bytes | English |

The five compressed resources total 50,849,516 bytes. Installation verifies the catalog size and SHA-256 digest before decompression, replaces only the selected language pack in a transaction, and reports progress to the settings UI. Removing an expanded pack deletes its SQLite entries and vacuums the database so the displayed disk usage falls with the actual file. The mini pack can be installed lazily again on the next lookup. The internal `mini` and `practical` editions are not exposed as product choices: settings present each available language only as **Installed** or **Not installed**, and mini packs do not contribute to the displayed installed-language count. Each row explicitly labels the bundled archive as **Compressed size**, while the footer labels the SQLite file as **Disk usage**.

Each installable archive may be assembled from several reviewed source layers while remaining one product-level install. The merge order is **primary practical dictionary**, **expanded general dictionary**, then a bounded **reviewed supplement**; personal terms remain a separate user-owned layer at lookup time. Exact duplicate senses are removed, but distinct source-authored senses are preserved. Every merged entry stores its own source priority and optional source attribution, so a primary entry wins the safe default order without disguising where a fallback entry came from. Strong local context evidence may still promote a better matching sense.

The shared catalog still covers all 28 product languages. Every installable pack must pass a reviewed common-vocabulary profile in addition to minimum size, attribution, and archive-integrity checks. The current profiles cover the five installable languages and prevent regressions such as silently losing basic Korean work terms or common English, Japanese, and Chinese words. The remaining 23 languages stay marked as planned until a redistributable primary source, a reviewed common-vocabulary profile, attribution, pack size, and human quality review are complete. A future remote catalog can use the same metadata and pack format without changing lookup storage.

## Sources and updates

The Korean archive uses the complete National Institute of Korean Language Basic Korean Dictionary XML snapshot created on 2026-06-19 as its primary layer. The reviewed snapshot at `spellcheck-ko/korean-dict-nikl` revision `42c0d01889f34536e9cf94fe57f62bd2055b1bde` yields 51,909 headwords and 76,833 senses. It carries dictionary-authored Korean definitions plus available English, Japanese, Simplified Chinese, Spanish, French, Russian, Indonesian, Arabic, Vietnamese, and Thai definition translations. Dictionary examples and every linked audio, image, and video asset are excluded because they have separate redistribution constraints.

The Korean-language Wiktionary dump dated 2026-08-04, extracted by Wiktextract/kaikki.org in August 2026, remains the expanded fallback layer. Its source-linked translation counts, example counts, and usage markers are used only to establish a safer default sense order; the linked translations and examples are not shipped as substitute definitions. A reviewed supplement can add source-authored modern terms from the National Institute of Korean Language Open Korean Dictionary without importing its roughly 1.2 million-sense, 1.8 GB XML corpus wholesale; the first supplement adds the three official senses of `초기화`. The merged archive contains 106,253 headwords and 153,321 distinct source senses. Plain exact substring matches inside a Korean token must cover that token completely through dictionary or recognized grammar spans. This prevents an unavailable word such as `이미지` from falling back to the unrelated prefix `이미`, while bounded inflection chains can analyze attached conversational forms without exposing arbitrary inner-word matches.

The English primary layer is built from Open English WordNet 2025 and joins each lexical form to its source synset definitions. The Simplified and Traditional Chinese primary layers are built together from the paired forms, pinyin, and English definitions in the CC-CEDICT release dated 2026-08-20. The Japanese primary layer uses the full English JMdict simplified release `3.6.2+20260817122448` rather than its common-word subset; all available written and reading forms are indexed. These are registered as the current primary sources; additional reviewed expansion layers can be merged without adding another install tier to settings. Up to twelve distinct, form-applicable senses are preserved per normalized headword. For example, `時間` keeps `time`, `hour`, and `period; class; lesson` as separate source senses instead of collapsing to the first gloss. Examples, audio, images, and separately licensed media are excluded from every pack.

JMdict's licence requires a regular update procedure, with monthly updates given as the example for dictionary services. Expanded packs therefore carry a dated version and source digest and must be reviewed and rebuilt regularly while they are distributed. Source and licence acknowledgements are deduplicated in a single collapsed section at the bottom of each lookup result and are also shown in the app's licence screen and `THIRD_PARTY_NOTICES.md`. The pack settings list shows only the available entry count and compressed archive size; one quiet **Sources and licences** action opens the dictionary-specific notices without repeating long source names on every language row. When one result combines entries from different sources, each sense keeps a short source label so its provenance remains identifiable without repeating the full licence text.

Run `npm run test:dictionaries` to validate catalog ordering, compressed sizes and hashes, minimum expanded coverage, the reviewed common-vocabulary profiles, per-source metadata, distinct senses per headword, supported parts of speech, definition limits, script-correct Chinese entries, and the reported lookup regressions. `npm run dictionary:build:korean -- ...` converts the complete Korean Basic Dictionary XML snapshot while excluding examples and media, `npm run dictionary:build -- ...` converts reviewed Wiktextract-style JSONL, `npm run dictionary:build:english -- ...` converts an Open English WordNet JSON export, `node scripts/build-jmdict-pack.mjs -- ...` converts a full JMdict simplified JSON release, and `npm run dictionary:build:chinese -- ...` generates both Chinese scripts from CC-CEDICT. `npm run dictionary:merge -- --input <primary> --input <expanded> ...` combines reviewed source layers in priority order and emits one practical archive. Every builder preserves distinct source senses within the configured pack limits.
