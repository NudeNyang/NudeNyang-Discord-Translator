# 웹 읽기 번역 범위 확대

2026-08-28 · 미공개 개발 변경 · 확장 0.7.11 · 본체 기능 정책 4

이 작업의 목표는 불필요한 차단을 줄이면서 기존 지원과 보호 경계를 유지하는 것이다.
로그인 사이트의 모든 화면이나 모든 웹메일을 완벽 지원한다고 판정한 문서는 아니다.
공개 릴리스·스토어 제출·원격 푸시는 하지 않는다.

## 변경 범위

| 영역 | 허용 범위 | 계속 제외하는 범위 |
| --- | --- | --- |
| 공개 문서 | 도움말·문서·뉴스 경로의 `settings`, `account`, `orders` 같은 단어 때문에 생기는 오차단 완화 | 미분류 개인 메시지 경로와 알려진 사적 서비스의 공개 수집 우회 |
| 공통 UI | 버튼·ARIA 버튼/탭/메뉴 항목·라벨·legend·입력란의 `aria-describedby` 설명·쿠키 안내 | 실제 입력값·선택값·편집기·숨긴 요소·명시적 번역 금지 |
| 계정·로그인·주문 | 구분 가능한 제목/항목명, 조작 문구, 연결된 설명, Product 이름·orderStatus | 임의 본문/값, 주소·Person·연락처·식별자·주문번호·금액으로 표시된 영역 |
| 가격 주변 | 별도 자식으로 구분된 세금 등의 설명 | 금액 노드와 숫자를 포함한 가격 조각 |
| Gmail 개발 경로 | 현재 열린 읽기 화면의 제목과 보이는 본문 | 목록·다른 메일·발신자/수신자 UI·초안·작성창·첨부 내용·숨긴 본문 |

계정 링크에는 사용자 이름이 표시될 수 있으므로, 계정 화면의 정적 UI를 허용해도
계정 목적지 링크를 일반 탐색 링크로 자동 승격하지 않는다. 기존 서비스별 닉네임·핸들
보호와 자연어 링크 번역, URL 표시·링크 목적지 보존을 유지한다.
HTML 의미와 서비스 경계에 근거한 제한이지, 임의 본문에 포함된 개인정보를 모두 탐지하거나
익명화하는 기능은 아니다. 표식 없는 이름이나 본문 속 민감 정보까지 제거한다고 안내하지 않는다.

## 확인한 원인과 공통 수정

- 경로의 모든 단어를 같은 민감 화면으로 간주해 공개 도움말도 차단했다. 일반 앵커와
  라우터 해시를 구분하고 공개 문서 경로와 정적 UI 전용 경로를 나눴다. 중첩된
  `/app/settings`, `/shop/account`와 언어 접두사는 제한을 우회하지 않는다.
- `form`, `button`, `label`, 쿠키·가격 컨테이너 제외가 안전한 안내까지 함께 막았다.
  `dom-policy.js`에 읽기용 UI 소유권을 추가하고 자식의 절대 보호 규칙은 다시 검사한다.
  하나의 Text 노드는 한 블록에만 속하며 기존 DOM·이벤트·입력 상태는 바꾸지 않는다.
- 입력 설명의 연결 속성이 나중에 생기면 입력 요소만 다시 검사해 형제 설명을 놓쳤다.
  `aria-describedby` 변경 때 해당 폼 또는 참조된 설명을 재검사한다. 입력값은 읽지 않는다.
- 정적 UI 전용 페이지에서는 전체 본문 진단과 임베드 제목 수집을 금지한다.
  SPA 경로 변경 직후, 정기 감시가 실행되기 전에도 현재 경로를 확인한다.

## 메일 경계와 동의

`messenger-adapters.js`의 `readingDocumentContext`는 서비스가 제공한 읽기 창·제목·본문
경계를 공통 계약으로 처리한다. 제목 하나와 보이는 본문이 있어야 하며, 보이는 읽기 창이
둘 이상이면 선택하지 않는다. 경계 탐색은 텍스트·입력값을 읽지 않는다.

Gmail 연결은 실제 화면에서 본문이나 주소를 읽지 않고 확인한 DOM 메타데이터를 사용했다.
서비스 선택자는 사적 서비스에서 허용할 데이터의 경계이며, 문장 누락을 고치는 사이트 예외가
아니다. 도메인 없는 합성 읽기 문서와 별도의 합성 Gmail 셸로 계약을 검사한다.
Outlook 및 그 밖의 메일은 아직 연결·실사용 검증을 완료하지 않았다. 확인 없이 일반 본문
수집을 열지 않으며, 테스트용 메일 화면 확보 후 같은 계약에 연결해야 한다.

통합 안내 한 화면에서 웹 동의 **v2**, 사적 읽기 동의 **v4**를 함께 저장한다.
기존 웹 v1·메신저 v3는 자동 확대하지 않는다. 이전 기록도 같은 화면에서 철회할 수 있다.
Firefox의 `personalCommunications`와 본체의 `messengerPolicyVersion: 4`를 별도로 확인한다.
F4·상단 전체 토글만 유지하며 메일 전용 스위치는 추가하지 않는다.

Gmail도 기존 사적 요청 경로를 사용한다. 실제 메일 URL·ID·주소를 요청 식별자로 보내지 않고
`messenger:gmail:<임의 nonce>`를 쓴다. 일반 창은 앱의 번역기·암호화 캐시·보관/삭제 정책을,
시크릿 창은 디스크 비사용·지원 공급자 제한을 그대로 따른다. OFF/철회/페이지 전환의
늦은 응답 보호와 재표시 캐시도 공유한다. 통합 안내는 28개 언어에 새 범위를 명시했다.

## 검증 기록

수정 전 실패를 확인한 검사:

- `node --test --test-name-pattern='읽기 정책' extension/test/site-adapters.test.mjs`: 3개 실패.
- `npm run test:e2e -- dom-regression.spec.mjs -g '읽기 정책'`: 최초 2개 실패.
- `node --test --test-name-pattern='메일 읽기|범위 확대' extension/test/messenger-adapters.test.mjs extension/test/global-state.test.mjs`: 3개 실패.
- `npm run test:e2e -- compatibility.spec.mjs -g '메일 읽기'`: 3개 실패.
- `npm run test:e2e -- dom-regression.spec.mjs -g '나중에 연결된'`: 1개 실패.

추가 검사는 공개 UI/계정 최소 HTML, 도메인 없는 메일 계약, 합성 Gmail 셸,
96개 UI 구조 조합의 중복·보호 텍스트 비읽기, 기존 2,048개 생성 구조,
메일 경계 탐색 비읽기, 동의/본체 버전/송신 서비스 검증, 전환·철회·재표시를 포함한다.
2026-08-28 실행 결과:

| 명령 | 결과 | 검증 범위 |
| --- | --- | --- |
| `npm test` | 747개 통과 | 웹 246·랜딩 37·확장 455·사전 9, 사전/팩 검증 포함 |
| `npm run test:e2e` | 148개 통과 | 실제 Chromium 확장 + 합성 사이트 + 모사 본체/번역 응답 |
| `npm run test:public` | 6개 통과 | 실제 공개 HTML 표본 + 고정 번역 응답 |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 443개 통과·46개 ignored | 본체 단위/통합 검사; ignored는 통과로 계산하지 않음 |
| `cargo test --manifest-path src-tauri/Cargo.toml live_local_model_completes_br_separated_public_prose -- --ignored --nocapture` | 1개 통과 | 실제 Hy-MT2와 고정 공개 일본어 문장 3개 |
| `npm run test:locales` | 통과 | 28개 언어 653/653 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | 통과 | Rust 포맷 |
| `git diff --check` | 통과 | 공백/패치 검사 |

Gmail 실제 화면에서는 읽기 경계의 DOM 구조만 확인했다. 실제 메일 내용과 실행 중인
확장·본체를 연결한 번역 검사는 아직 하지 않았다. 위 실제 모델 검사는 Gmail 연동 검사가
아니다. 민감하지 않은 테스트 메일에서 제목/본문만 번역되는지, 철회·화면 전환·캐시 복원이
정상인지 추가 확인해야 한다. Outlook은 실제 읽기 화면의 안전한 경계 확인과 구현부터
남아 있다. 따라서 메일 전체 지원 목표는 진행 중이다.

## 로컬 빌드 확인

- 구현 커밋: `06fa110` (`codex/web-translation-dom-refactor`).
- `npm run tauri:build`: 성공. 본체 `0.7.3-beta`의 최신 실행 파일을 생성했다.
- 프로젝트 바로가기의 대상·작업 폴더·아이콘을 최신 실행 파일에 동기화했다.
  해당 경로의 기존 앱만 종료하고 바로가기로 실행해 경로·제품 버전·창 응답을 확인했다.
- `npm run extension:personal`, `npm run extension:chromium`, `npm run extension:firefox`:
  개발용 `0.7.11` 생성 성공. 두 압축 파일의 버전과 변경된 주요 제품 파일 8개의
  원본 일치를 검사했다. 기존 `0.7.10-share` 파일은 기존 체크섬과 일치한다.
- 사용 중인 브라우저 확장을 자동 재로드하거나 사용자 대신 확대 동의를 승인하지 않았다.
  새 본체와 개발용 확장을 함께 사용해야 하며, 확대된 안내에 한 번 동의해야 적용된다.
- 원격 푸시·공개 릴리스·스토어 제출은 하지 않았다.

정책 참고: [Chrome 고지 요건](https://developer.chrome.com/docs/webstore/program-policies/disclosure-requirements),
[Firefox 데이터 전송·동의 요건](https://extensionworkshop.com/documentation/publish/add-on-policies/).
메일 번역 자체를 금지한다는 규칙은 아니지만 데이터 범위·전송·저장을 명확히 고지해야 한다.
이 설계나 테스트 통과가 스토어 심사 통과를 보장하지는 않는다.
