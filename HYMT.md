# Hy-MT2 적용 기록

## 선택

- 기본: `tencent/Hy-MT2-1.8B-GGUF`, `Q4_K_M`
- 품질 우선: `tencent/Hy-MT2-7B-GGUF`, `Q4_K_M`
- 실험: `SandLogicTechnologies/translategemma-4b-it-GGUF`, `Q4_K_M`
- 실행기: `llama.cpp`의 로컬 `llama-server`
- 클라우드 선택형: 사용자가 키를 제공하는 DeepL
- 지원 우선순위: 한국어 → 일본어 → 영어 → 중국어
- 중국어 표시 언어: 간체와 번체를 별도 선택

Hy-MT2는 번역 특화 모델이고 네 언어를 모두 지원한다. 1.8B Q4 모델은 1.13GB, 7B Q4
모델은 4.62GB다. 모델과 `llama.cpp` 모두 공개 배포에 적합한 라이선스를 사용한다. 기본
Windows 배포 ZIP에는 1.8B 모델을 내장하고, 7B는 패키징 옵션으로 포함하거나 최초 선택 때
공식 저장소에서 받는다. Git 저장소 자체에는 대용량 가중치를 넣지 않는다.

## 무결성과 캐시

다운로드 완료 조건은 파일 크기와 공식 Git LFS SHA-256이 모두 일치하는 것이다. 검증된
해시는 사용자 캐시 모델 옆 마커 파일에 기록하고 이후 실행에서 다시 수 GB를 해싱하지 않는다.
읽기 전용일 수 있는 번들 모델은 실행 전에 원본 SHA-256과 직접 비교한다.

- 1.8B Q4: `dc5f44fcf1fa496ee7ad725982c0c8c553a4de00259b53af84c4b89fb0c06699`
- 7B Q4: `9f96256500f3fc1ab4d64336b58f52a949a95ad7516b0c229476eef782f9f77b`

번역 캐시 이름에는 모델 크기, 양자화, 프롬프트 버전을 포함한다. 모델이나 프롬프트를 바꾸면
이전 결과를 재사용하지 않으며 다른 번역 엔진의 캐시와도 섞이지 않는다.

TranslateGemma 4B 실험 모델은 2,489,909,312바이트이며 SHA-256
`526747309109c016db547c6fc1c7b0c9c286b5e7a7556827b5419fd9543a09cd`로 고정한다.
기본 배포와 Git 저장소에는 넣지 않고 사용자가 선택한 경우에만 별도 폴더로 내려받는다.
Hy-MT2 캐시와 섞이지 않도록 독립된 `translategemma` 이름 공간을 사용한다.

## GPU와 시스템 메모리

로컬 모델 실행 장치의 기본값은 `자동`이다. 먼저 GPU 오프로딩을 시도하고 VRAM 부족 또는
GPU 초기화 실패로 서버가 종료되면 같은 모델을 CPU/RAM 전용 모드로 한 번 다시 시작한다.
CPU 실행은 시스템 RAM을 사용하므로 GPU가 없어도 동작하지만 속도는 느려질 수 있다. CPU 시도는
문맥 크기를 2,048토큰으로 제한해 시스템 RAM 사용량이 불필요하게 커지는 것을 막는다.

설정 화면은 선택한 모델 파일 크기와 실행 장치별 예상 메모리 사용량을 표시한다. 2026-08-12
Windows GPU 프로세스 메모리 카운터로 `ctx-size 8192`, GPU 자동 오프로딩 상태의 Hy-MT2
1.8B 전용 VRAM을 측정한 값은 약 1.68GiB였으며, UI에는 반올림한 약 1.7GB로 안내한다.
CPU/RAM 전용 모드에서는 같은 모델을 약 2.0GB RAM으로 안내한다. 값은 모델 파일 외에 KV 캐시와
실행 버퍼를 포함한 근사치이므로 llama.cpp 버전, GPU 드라이버와 실행 환경에 따라 달라질 수 있다.

전체 RAM이 12GB 미만이거나 CPU 모드의 가용 RAM이 예상 사용량보다 적으면 `Hy-MT2 1.8B`,
`CPU/RAM 전용`, `로컬 모델 예열 유지 끄기`를 묶은 저사양 권장 설정을 제공한다.

## 프롬프트와 보호 대상

`llama.cpp`의 OpenAI 호환 채팅 엔드포인트를 사용해야 GGUF에 포함된 공식 채팅 템플릿이
적용된다. 일반 completion 엔드포인트는 이 모델에서 깨진 토큰을 생성하는 것을 실제로
확인해 사용하지 않는다.

번역 전 `@everyone`, 사용자 멘션, 채널 태그, URL, 이모지, 문자 이모티콘을 임시 토큰으로
치환한다. 1.8B 모델은 토큰 보존 규칙을 프롬프트에 길게 적으면 그 규칙까지 번역 결과로
내보내는 경우가 있어, 임시 토큰을 기준으로 문자열을 로컬에서 나누고 텍스트 조각만 모델에
보낸다. 모델 프롬프트는 공식 예제와 같은 최소 번역 지시만 사용하고 결과를 합칠 때 원문
요소를 정확히 복원한다. 프롬프트 변경 전 오염된 캐시는 `official-minimal-v2` 이름 공간과
자동으로 분리된다.

## 2026-08-05 실측

Hy-MT2 1.8B Q4를 실제 `llama.cpp` 서버로 실행했다.

| 입력 | 결과 요약 | 시간 |
|---|---|---:|
| 일본어 → 한국어 | 의미는 전달했지만 `肉まん`에 오역 발생 | 1.47초, 최초 기동 포함 |
| 영어 → 한국어 | 자연스러운 결과 | 0.10초, warm |
| 중국어 간체 → 한국어 | 자연스러운 결과 | 0.08초, warm |

실제 Discord 재검사에서는 메시지 8개와 채널명 28개를 새 프롬프트로 번역했고, 새로 저장된
36개 캐시 결과에서 프롬프트 지시문이 섞인 항목은 0개였다.

1.8B를 최종 품질 기준으로 확정한 것은 아니다. UI Automation으로 정확히 추출한 실제 Discord
문장 100~200개를 사용해 1.8B, 7B, DeepL을 사람 평가로 비교한 뒤 자동 선택 기준을 정한다.

## 실험 모델

TranslateGemma 4B는 기존 Hy-MT2를 대체하지 않는 비교용 선택지다. 현재 llama.cpp 서버의
자동 템플릿 파서가 구조화 메시지 템플릿을 초기화하지 못하므로, GGUF에 저장된 공식 텍스트
번역 템플릿을 동일하게 렌더링해 completion 엔드포인트에 전달한다. Hy-MT2용 지시 프롬프트나
말투 재작성 단계는 적용하지 않지만, 원문에서 판별한 존댓말·반말과 사용자가 고정한 말투 설정은
공식 템플릿 안의 사회적 말투 지시로 전달한다. 말투 지시가 없던 이전 결과를 재사용하지 않도록
TranslateGemma 캐시를 `register-aware-v2` 이름 공간으로 분리한다. 모델에는 Apache-2.0이 아닌
별도의 Gemma Terms of Use가 적용되므로 설정과 제3자 고지에서 이를 명확히 표시한다. 실제
Discord 문장으로 품질과 응답 시간을 측정한 뒤 기본 모델 변경 여부를 결정한다.

2026-08-11 Q4_K_M 실측에서 한국어 `너구리가 다람쥐를 만났어`를 일본어
`キツネとリスが出会った`로 번역했다. 번역 경로와 대상 언어 제어는 정상 작동했지만 `너구리`를
`여우`로 오역했으므로 현재 품질 기준에서는 기본 모델로 승격하지 않는다.

2026-08-11 말투 보강 후 한국어 반말 `오늘 같이 게임할래?`는 일본어 반말
`今日、一緒にゲームしない？`로, 한국어 존댓말 `오늘 같이 게임하시겠어요?`는 일본어 존댓말
`今日、一緒にゲームをしませんか？`로 구분했다. 일본어 자동 판별은 `ですか`, `ますか`,
`ませんか`, `でしょうか` 의문형도 존댓말로 처리한다.

## 보류

- Papago 웹 자동화: 공식 제품 연동 방식이 아니고 페이지 변경·차단에 취약해 보류
- Hy-MT2 1.25-bit: Windows 배포 경로가 충분히 안정화될 때 다시 검토

## 출처

- <https://huggingface.co/tencent/Hy-MT2-1.8B-GGUF>
- <https://huggingface.co/tencent/Hy-MT2-7B-GGUF>
- <https://github.com/Tencent-Hunyuan/Hy-MT2>
- <https://huggingface.co/SandLogicTechnologies/translategemma-4b-it-GGUF>
- <https://ai.google.dev/gemma/terms>
- <https://github.com/ggml-org/llama.cpp>

내장 배포 시 각 GGUF 저장소의 Apache-2.0 원문과 Tencent 저작권 고지를 변경 없이 함께
제공한다. 저장된 원문은 `licenses/Hy-MT2-1.8B-GGUF-LICENSE.txt`와
`licenses/Hy-MT2-7B-GGUF-LICENSE.txt`에서 확인할 수 있다.
