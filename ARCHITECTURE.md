# 렌더링 및 배포 경계

## Tauri 전환 기준

새 기본 앱 셸은 Tauri 2 + Rust다. Rust가 설정창, 트레이, 단일 인스턴스와 엔진 프로세스
생명주기를 소유하고, Python은 JSON Lines 프로토콜 뒤의 헤드리스 엔진으로 동작한다. Python
엔진에는 DOM/CDP 번역, Hy-MT2, 캐시와 이미지 OCR이 남는다. OCR은 레거시가 아니라 기본
이미지 번역 기능이므로 전환 완료 후에도 사이드카로 유지할 수 있다.

세부 책임과 단계, 완료 조건은 [TAURI_MIGRATION.md](TAURI_MIGRATION.md)를 기준으로 한다.
이 문서 아래의 PySide6 오버레이 구조 설명은 전환 기간 동안 유지되는 기존 구현 기록이다.

## 운영체제 경계

공개 배포는 현재 Windows 10/11 x64만 지원한다. macOS용 실행 파일은 아직 없으며,
Apple Silicon 지원을 나중에 추가할 수 있도록 다음 항목을 공통 코드 밖의 플랫폼 경계로 둔다.

- llama.cpp 실행 파일 이름, 번들 내부 위치와 프로세스 생성 옵션
- 전역 단축키의 네이티브 백엔드
- Discord를 원격 디버깅 포트와 함께 시작하는 방법
- Release 자산 이름, 패키징, 코드 서명·공증과 자동 업데이트 설치 방식

`discord_translate_overlay.platforms`는 현재 이 계약과 지원 여부를 제공한다. Windows 구현은
현재 동작을 유지하고, macOS 계약은 `.app/Contents/Resources`의 `llama-server` 위치와
Apple Silicon 릴리스 이름을 미리 고정한다. 아직 구현하지 않은 macOS 전역 단축키와 자동
업데이트는 지원 여부가 `False`이므로 Windows API나 PowerShell 경로로 잘못 진입하지 않는다.

번역기, Hy-MT2 GGUF 모델, CDP 클라이언트, DOM 변환, 설정과 캐시는 운영체제와 독립적인
코어로 유지한다. 새 플랫폼 코드는 이 코어에서 `sys.platform`, Win32 API 또는 `.exe` 이름을
직접 분기하지 않고 플랫폼 계층을 통해 접근한다. 세부 로드맵은 [MACOS.md](MACOS.md)에 있다.

Discord 디버그 재시작도 플랫폼 서비스로 분리한다. UI는 최초 동의, 15초 카운트다운과
재시작 결과만 관리하며 Windows 구현이 프로세스 확인·종료·실행을 담당한다. CDP 연결 실패는
2회 연속 확인해야 재시작 후보가 되고, 카운트다운 시작 때 기록한 PID와 실행 직전 PID가
다르면 요청을 폐기한다. 한 번의 번역 활성화에서 자동 재시작은 최대 한 번만 수행한다.

## 기존 외부 오버레이 구조 (레거시)

Nude Translator의 기본 구조는 Discord 프로세스 밖에서 동작한다.

- Discord API, 사용자 토큰, self-bot을 사용하지 않는다.
- Discord 설치 파일과 Electron DOM을 수정하지 않는다.
- Windows UI Automation으로 화면에 노출된 텍스트와 좌표를 읽는다.
- UI Automation이 제공하지 못하는 영역만 로컬 화면 OCR로 보완한다.
- 번역 결과는 별도의 클릭 통과형 Windows 오버레이에만 그린다.

번역 캐시는 최근 4,096건을 보관하는 메모리 LRU를 1차로 사용하고 SQLite를 2차 영구
저장소로 사용한다. 새 결과는 메모리에 먼저 반영하고 단일 백그라운드 작성자가 SQLite에
기록한다. 종료 시 대기 중인 기록을 모두 비운 뒤 DB를 닫는다.

이 구조는 기존 GitHub 공개 빌드의 기본 모드였다. Tauri 전환 뒤에는 회귀 비교와 복구를 위해
일시적으로 유지하며 새 기능의 기본 실행 경로로 사용하지 않는다.

## 메시지 단위 합성 렌더러

조각별 투명 구멍 방식은 이모티콘과 임베드가 많은 메시지에서 원문 재노출,
과도한 여백, 번역문 잘림을 만들었다. 메시지 단위 합성 렌더러는 다음 순서로
처리한다.

1. UI Automation이 이모티콘과 구조화 요소를 종류별로 표시한다.
2. 화면 프레임에서 작은 인라인 이모티콘 픽셀을 캡처한다.
3. 원문 텍스트 영역을 로컬 Discord 배경색으로 한 번에 덮는다.
4. 번역문을 하나의 문단으로 배치한다.
5. 캡처한 이모티콘을 번역문 다음 흐름에 작은 이미지로 다시 배치한다.
6. 멘션과 링크처럼 실제 클릭 대상인 구조화 요소만 최소한의 투명 영역으로 보존한다.

OCR 조각 사이의 단순한 공백은 더 이상 이모티콘으로 추정하거나 투명하게 뚫지 않는다.

## DOM 방식의 공개 및 안전 경계

Tauri 앱의 기본 번역 경로는 DOM 방식이다. DOM 컨트롤러 소스와 빌드는 GitHub 저장소와
Release에 포함할 수 있지만, Discord가 공식 지원하는 확장 방식이 아니며 클라이언트 업데이트로
언제든 동작이 깨질 수 있다. 다음 조건을 적용한다.

- 실시간 번역은 기본으로 자동 활성화하지 않으며 사용자가 위험 고지를 확인한 뒤 켠다.
- 사용자 토큰, 비공식 API, self-bot, 메시지 자동 전송은 DOM 모드에서도 금지한다.
- Discord 서버 데이터와 설치 파일을 수정하지 않고 현재 렌더링 세션의 표시만 바꾼다.
- 설정 화면과 README에 Discord 약관 위반 및 계정 제재 가능성을 명확히 알린다.
- Discord 파일을 영구 패치하는 방식보다 세션 한정 실험을 우선한다.
- 전환 기간에는 문제가 생겼을 때 레거시 외부 오버레이 빌드로 복구할 수 있다.

DOM 방식의 공개는 안전성을 보증한다는 뜻이 아니다. 기능이 작동하지 않거나 Discord 정책이
바뀌면 배포를 중단하거나 제거할 수 있다.
