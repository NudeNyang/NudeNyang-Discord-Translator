# 렌더링 및 배포 경계

## 기본 공개 구조

Nude Translator의 기본 구조는 Discord 프로세스 밖에서 동작한다.

- Discord API, 사용자 토큰, self-bot을 사용하지 않는다.
- Discord 설치 파일과 Electron DOM을 수정하지 않는다.
- Windows UI Automation으로 화면에 노출된 텍스트와 좌표를 읽는다.
- UI Automation이 제공하지 못하는 영역만 로컬 화면 OCR로 보완한다.
- 번역 결과는 별도의 클릭 통과형 Windows 오버레이에만 그린다.

번역 캐시는 최근 4,096건을 보관하는 메모리 LRU를 1차로 사용하고 SQLite를 2차 영구
저장소로 사용한다. 새 결과는 메모리에 먼저 반영하고 단일 백그라운드 작성자가 SQLite에
기록한다. 종료 시 대기 중인 기록을 모두 비운 뒤 DB를 닫는다.

이 구조는 GitHub 공개 빌드의 기본 모드이며, 설치 직후에는 이 방식으로 시작한다.

## 메시지 단위 합성 렌더러

조각별 투명 구멍 방식은 이모티콘과 임베드가 많은 메시지에서 원문 재노출,
과도한 여백, 번역문 잘림을 만들었다. 메시지 단위 합성 렌더러는 다음 순서로
처리한다.

1. UI Automation이 이모티콘과 구조화 요소를 종류별로 표시한다.
2. 화면 프레임에서 작은 인라인 이모티콘 픽셀을 캡처한다.
3. 원문 텍스트 영역을 로컬 Discord 배경색으로 한 번에 덮는다.
4. 번역문을 하나의 문단으로 배치한다.
5. 캡처한 이모티콘을 번역문 다음 흐름에 작은 이미지로 다시 배치한다.
6. 멘션과 링크처럼 실제 클릭 대상인 구조화 요소만 최소한의 투명 영역으로 보존한다.

OCR 조각 사이의 단순한 공백은 더 이상 이모티콘으로 추정하거나 투명하게 뚫지 않는다.

## 실험적 DOM 방식의 공개 및 안전 경계

외부 합성 방식으로도 제품 목표를 달성할 수 없다고 사용자와 명시적으로 판단한
경우를 위해 DOM 방식을 실험 기능으로 제공한다. DOM 컨트롤러 소스와 빌드는 GitHub
저장소와 Release에 포함할 수 있지만, Discord가 공식 지원하는 확장 방식이 아니며 클라이언트
업데이트로 언제든 동작이 깨질 수 있다. 다음 조건을 적용한다.

- DOM 기능은 기본으로 자동 활성화하지 않으며 사용자가 위험 고지를 확인한 뒤 선택한다.
- 사용자 토큰, 비공식 API, self-bot, 메시지 자동 전송은 DOM 모드에서도 금지한다.
- Discord 서버 데이터와 설치 파일을 수정하지 않고 현재 렌더링 세션의 표시만 바꾼다.
- 설정 화면과 README에 Discord 약관 위반 및 계정 제재 가능성을 명확히 알린다.
- Discord 파일을 영구 패치하는 방식보다 세션 한정 실험을 우선한다.
- 공개 릴리스에서는 외부 오버레이 모드로 즉시 돌아갈 수 있는 선택지를 유지한다.

DOM 방식의 공개는 안전성을 보증한다는 뜻이 아니다. 기능이 작동하지 않거나 Discord 정책이
바뀌면 배포를 중단하거나 제거할 수 있다.
