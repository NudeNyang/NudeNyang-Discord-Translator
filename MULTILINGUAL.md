# 28개 채팅 언어 지원

## 제품 언어 카탈로그

텍스트 채팅의 수신 번역, 보내는 메시지 번역, 표시 언어 선택은 다음 28개 BCP 47 계열 코드를
공통 카탈로그로 사용한다.

| 코드 | 표시 이름 | 코드 | 표시 이름 |
|---|---|---|---|
| `ko` | 한국어 | `en` | English |
| `ja` | 日本語 | `zh` | 简体中文 |
| `zh-Hant` | 繁體中文 | `pt-BR` | Português (Brasil) |
| `es-419` | Español (Latinoamérica) | `de` | Deutsch |
| `fr` | Français | `id` | Bahasa Indonesia |
| `hi` | हिन्दी | `vi` | Tiếng Việt |
| `pl` | Polski | `ru` | Русский |
| `uk` | Українська | `tr` | Türkçe |
| `ar` | العربية | `it` | Italiano |
| `nl` | Nederlands | `ms` | Bahasa Melayu |
| `th` | ไทย | `fil` | Filipino |
| `bn` | বাংলা | `ur` | اردو |
| `ta` | தமிழ் | `fa` | فارسی |
| `he` | עברית | `cs` | Čeština |

`zh-Hans`, `pt`, `es`는 이전 설정이나 공급자 입력을 읽기 위한 별칭으로만 허용하고 저장할 때는
각각 `zh`, `pt-BR`, `es-419`로 정규화한다. 지원하지 않는 코드는 표시 언어에서 `ko`, 보내는
언어에서 `auto`로 안전하게 되돌린다.

언어 선택 메뉴는 `ko`, `en`, `ja`, `zh`, `zh-Hant`를 맨 앞에 고정한다. 이후에는 Discord 이용
규모가 큰 브라질·인도·독일·러시아 언어권과 라틴아메리카·인도네시아처럼 인구가 큰 언어권을
우선해 `pt-BR`, `hi`, `es-419`, `de`, `ru`, `id`, `fr`, `tr`, `ar`, `vi`, `it`, `pl`, `uk`,
`ms`, `nl` 순으로 표시한다. 1차 확장 언어는 동남아·남아시아·중동의 지역 균형과 로컬 모델의
명시적 지원을 함께 고려해 `th`, `fil`, `bn`, `ur`, `ta`, `fa`, `he`, `cs` 순으로 둔다. 특히
Discord가 베트남어와 태국어를 모두 정식 로케일로 취급하는데 한쪽만 제공하는 불균형을 해소한다.
이 순서는 감지 후보 순서와 분리해 감지 결과에는 영향을 주지 않는다.

## 인터페이스 언어

설정창, 트레이 메뉴, Discord 안의 보내는 메시지·표시 언어 메뉴, 보낸 메시지 원문 전환,
이미지 번역 버튼은 시스템 언어 자동 선택과 위 28개 언어를 공통으로 지원한다. 인터페이스 언어와
메시지 번역 대상 언어는 서로 독립된 설정이며, 인터페이스를 바꿔도 메시지 감지 후보나 번역
공급자 선택은 바뀌지 않는다.

언어 선택 제목과 자동 선택 항목은 번역하지 않고 모든 인터페이스에서 각각 `UI Language`,
`Auto (System)`으로 통일한다. 이는 의도적인 복구용 영어 고정 정책이다. 시스템 언어 자동 감지가
정상이라면 사용자가 이 항목을 찾을 필요가 거의 없지만, 감지 오류로 읽을 수 없는 인터페이스 언어가
선택됐을 때도 외국인 사용자가 언어 설정과 자동 선택 항목을 즉시 찾아 복구할 수 있어야 하기 때문이다.
자동 선택은 운영체제/웹뷰의 BCP 47 언어 태그를 다음처럼 정규화한다.

- `zh-TW`, `zh-HK`, `zh-MO`, `zh-Hant*` → `zh-Hant`; 나머지 `zh*` → `zh`
- `pt*` → `pt-BR`, `es*` → `es-419`, 이전 인도네시아어 코드 `in*` → `id`
- 나머지는 기본 언어 하위 태그를 제거한 뒤 28개 카탈로그와 비교하고, 지원하지 않으면 영어를 쓴다.

설정창의 정적 문구는 한국어 키와 검수된 영어 의미 기준문을 원본으로 관리한다. 영어·일본어·간체
중국어는 기존 수동 사전을 유지하며 나머지 24개 언어는 `web/ui-locales.mjs`와 Rust 주입 UI용
`web/ui-locales.json`을 한 번에 생성한다. 신규 언어 사전에 없는 정적 문구는 다른 언어와 섞지 않고
영어 기준문으로 되돌린다. 숫자·모델명·파일 크기가 들어가는 34종 런타임 문구도 자리표시자를 가진
동일한 사전에 포함해 28개 언어로 렌더링한다. 알려지지 않은 백엔드 오류는 원문 오류를 다른 언어로
노출하지 않고, 선택한 인터페이스 언어의 일반 오류 안내와 개인정보를 포함하지 않는 영문 세부 정보만
표시한다.

런타임 상태에서 모델명과 `준비 중`·`사용 중` 같은 상태를 조합할 때는 양쪽을 각각 현지화한다.
`원문`은 고유 모델명이 아니므로 28개 인터페이스 언어의 사전 항목을 사용하며 한글을 그대로 노출하지
않는다.

28개 인터페이스 언어는 설정창의 열·버튼·메뉴 위치를 동일하게 유지하기 위해 문서 레이아웃을
`dir=ltr`로 고정한다. 다만 언어 선택 목록의 아랍어·우르두어·페르시아어·히브리어 항목은 오른쪽
정렬하고, 나머지 언어와 검색창은 왼쪽 시작선을 유지한다. RTL 글자 자체의 양방향 텍스트 처리는
브라우저에 맡긴다.

언어 항목이 많아졌으므로 표시 언어, 보내는 언어, 인터페이스 언어 선택창과 트레이·Discord 언어
메뉴에 검색창을 둔다. 검색은 대소문자와 라틴 악센트를 무시하며 다음 네 값을 함께 찾는다.

- 현지어 이름: `Português (Brasil)`, `العربية`
- 제품 코드: `pt-BR`, `zh-Hant`
- 대표 국가 코드: `JP`, `IN`, `VN`
- 영어 이름: `Brazilian Portuguese`, `Traditional Chinese`

Discord의 짧은 언어 표시는 국가 코드가 아니라 언어 코드를 우선한다. 따라서 일본어는 `JP`가 아닌
`JA`로 표시한다. 검색에서는 언어 코드 `ja`와 대표 국가 코드 `JP`를 모두 허용한다. 두 글자 검색어가
언어·국가 코드와 정확히 일치하면 일반 이름의 부분 일치보다 코드 결과를 우선한다.

메뉴를 열면 검색창에 포커스를 두고, 검색어가 없으면 기존 우선순위를 그대로 유지한다. 결과가 없으면
현재 인터페이스 언어로 빈 결과 안내를 표시한다.

CLI·번역 서비스 연결 동작은 번역문 길이에 따라 카드 폭이 흔들리지 않도록 36px 정사각형의 링크·
링크 해제 아이콘 버튼으로 표시한다. 버튼의 `aria-label`과 툴팁은 현재 인터페이스 언어로 번역한다.
DeepL은 API 키를 직접 입력·변경해야 하므로 기존 인라인 키 입력창을 유지하며 별도의 키 변경 아이콘을
두지 않는다. 연결된 DeepL을 해제하는 동작만 다른 공급자와 같은 링크 해제 아이콘을 사용한다.

설정창은 네이티브 최소 폭을 760 논리 픽셀로 고정한다. 이 폭에서는 상단 6개 분류를 가로 스크롤이
없는 3열×2행으로 배치하고 긴 번역은 항목 안에서 줄바꿈한다. 760px보다 넓으면 기존 좌측 탐색
레이아웃을 유지한다.

### 인터페이스 번역 갱신과 검증

```powershell
# 빠진 키만 생성하며 기존 번역은 덮어쓰지 않음
node scripts/generate-ui-locales.mjs

# 24개 생성 사전의 완전성, 한글 잔존, 자리표시자, 보호 토큰, 길이 이상치 검사
npm run test:locales

# 검색·자동 언어 판정·설정 정규화를 포함한 UI 회귀 검사
npm test
```

생성기는 영어 기준문을 번역하고 한국어 키는 문맥으로만 제공한다. 사전은 문구 수가 정확히 일치하고
빈 값과 한글 잔존이 없으며 `{language}`, `{part}`, `{total}` 같은 자리표시자를 보존해야만 기록된다.
`Discord`, 모델명, API/CLI, GPU/CPU/RAM/VRAM, 단축키 토큰도 그대로 유지한다. 자동 검사는 구조적
회귀와 대표적인 위험을 막지만 지역별 어감까지 보증하지 않으므로, 실제 사용자 제보로 문구를 수정할
때는 해당 키만 고치고 같은 검사를 다시 실행한다.

## 감지 정책

- 한글, 가나, 데바나가리, 태국, 벵골, 타밀, 히브리 문자는 문자 체계가 충분히 나타날 때 즉시
  판정한다. 아랍 문자를 공유하는 아랍어·페르시아어·우르두어는 문자만으로 단정하지 않는다.
- 간체·번체는 서로 다른 힌트 글자를 사용하고, 한자만 있는 짧은 문장은 최근 중국어/일본어
  문맥이 없으면 `und`로 보류한다.
- 나머지는 Lingua 1.8.0을 제품의 27개 기본 언어 모델로 제한해 사용한다. 기본 승인 기준은
  신뢰도 `0.35`, 1·2위 격차 `0.17`이다.
- 인도네시아어/말레이어와 러시아어/우크라이나어는 1·2위가 같은 혼동군일 때만 신뢰도 `0.50`,
  격차 `0.03`을 별도 적용한다.
- URL, Discord 멘션·채널 태그·커스텀 이모지, 코드 펜스 표식은 통계 입력에서 제거한다.
- `gg`, `lol`, `nice`, `no`, `si`, 숫자·URL만 있는 메시지는 억지로 판정하지 않는다. 기존 제품
  회귀를 막기 위해 `hello`, `please`, `welcome`처럼 명백한 영어 신호는 짧아도 영어로 유지한다.

오판보다 `und` 보류를 우선한다. 자동 감지가 보류되면 최근 채널 언어 또는 사용자가 선택한
언어를 사용하며, 감지 결과가 달라져도 다른 원문 언어의 캐시를 재사용하지 않는다.

Discord의 채널명과 카테고리명은 메시지와 분리된 서버 탐색 문맥으로 묶어 감지한다. `rules`,
`chat`, `art`처럼 단독 판정이 어려운 짧은 이름은 같은 서버에서 함께 보이는 탐색 항목의 문자 체계와
언어를 사용하며, 채널을 이동해도 서버가 같으면 이 문맥을 유지한다. 서버가 바뀌면 탐색 문맥을
초기화하고, 탐색 문맥은 메시지 언어 추정에 사용하지 않는다. 카테고리 DOM 수집은 Discord의 표시
언어별 `aria-label` 문구가 아니라 언어와 무관한 역할과 구조를 기준으로 한다.

## 번역 공급자 능력표

`src-tauri/src/language.rs`가 공급자별 원문/대상 코드를 한 곳에서 관리하고 모든 조합을 테스트한다.

| 공급자 | 28개 언어 코드 | 검증 수준 |
|---|---|---|
| Hy-MT2 1.8B/7B | 제품 코드와 언어 이름 | 1.8B 54방향, 7B 신규 8개 언어 16방향 로컬 실번역 스모크 완료 |
| TranslateGemma 4B | 제품 코드, 중국어 `zh-CN`/`zh-TW` | 신규 8개 언어 16방향 로컬 실번역 스모크 완료 |
| ChatGPT·Claude·Gemini CLI | 제품 코드와 언어 이름 | 프롬프트·스키마 계약 테스트; 계정별 실호출은 별도 |
| DeepL | 원문 `ZH/PT/ES`, 대상 `ZH-HANS/ZH-HANT/PT-BR/ES-419` 등 | 28개 요청 매핑 테스트; API 키별 실호출은 별도 |

필리핀어는 제품·UI·상용 CLI에서 현대 BCP 47 코드 `fil`을 유지하되, Hy-MT2와 DeepL에는 각
공급자가 명시한 `tl`/`TL` 코드로 변환한다. 코드가 다르다는 이유로 다른 언어로 대체하지 않는다.

외부 공급자의 언어 목록이나 계정 권한이 바뀌면 요청이 실패할 수 있으므로 원문을 유지하고 오류를
표시한다. 존재하지 않는 언어로 조용히 대체하지 않는다.

## 품질 안전장치

- 원문 반복, 빈 결과, 비정상적인 장문 환각, 번역 거부 문구를 캐시하지 않는다.
- 한국어·일본어·중국어·힌디어·태국어·벵골어·타밀어·아랍어·우르두어·페르시아어·히브리어·
  러시아어·우크라이나어 대상에서 필요한 문자 체계가 전혀 없으면 한 차례 교정 번역하고,
  그래도 실패하면 오역 대신 원문을 유지한다.
- 중국어→한국어에서 남은 `繁體/繁体/簡體/简体/中文`은 의미가 고정된 언어명만 결정론적으로
  정리한다.
- 한국어 `-(으)ㄹ래(요)?`가 독일어·포르투갈어·러시아어·우크라이나어·네덜란드어에서
  1인칭 복수 제안으로 바뀌는 회귀를 별도 검사하고 2인칭 질문으로 보정한다.
- 마크다운, 줄바꿈, 코드 블록, 멘션, URL, 이모지는 모델 입력 전 보호하고 결과에 정확히 복원한다.
- 필리핀어에 영어 인사말이 섞이거나 한국어 게임 초대가 우르두어의 음식/먹기 표현으로 바뀌는
  확인된 소형 모델 회귀를 교정하고 별도 테스트한다.
- 한국어 게임 초대가 태국어·벵골어·우르두어·타밀어·페르시아어·히브리어·체코어에서 화자의
  제안으로 바뀌는 확인된 회귀와 타밀어 `서버`가 한국어 `서비스 센터`로 바뀌는 회귀는 모든
  로컬·CLI·API 엔진이 공유하는 보수적 후처리 계층에서 교정한다.
- 프롬프트/후처리 변경은 `meaning-preserving-v12` 캐시 이름 공간으로 격리한다.

## OCR 범위

28개 언어는 텍스트 채팅 기준이다. 현재 이미지 번역은 PP-OCRv6-small과 한국어 PP-OCRv5
인식기의 고정 문자셋 범위 안에서만 제공한다.

- 문자셋 표본 100%: `ko`, `en`, `ja`, `zh`, `zh-Hant`, `pt-BR`, `es-419`, `de`, `fr`,
  `id`, `pl`, `tr`, `it`, `nl`, `ms`, `fil`, `cs`
- 부분(광고하지 않음): `vi` 91%
- 현재 인식기 미지원: `hi`, `ru`, `uk`, `ar`, `th`, `bn`, `ur`, `ta`, `fa`, `he`

100%는 글자 사전에 있다는 뜻이며 실제 이미지 정확도 보장은 아니다. 공통 감지 모델은 글자 영역을
찾을 수 있어도 인식 문자셋에 없는 문자는 복원할 수 없다. 이 문자 체계들은 해당 PaddleOCR
인식 모델을 추가하고 렌더링 이미지 평가를 통과하기 전까지 OCR 지원으로 표시하지 않는다.

## 재현 방법

```powershell
# 빠른 감지 회귀
powershell -ExecutionPolicy Bypass -File scripts/multilingual-benchmark.ps1 -Mode Detection

# 설치된 Hy-MT2 1.8B를 실제로 실행하는 양방향 번역·형식 검사
powershell -ExecutionPolicy Bypass -File scripts/multilingual-benchmark.ps1 -Mode Translation

# 고정 OCR 모델의 언어별 문자셋 범위 검사
powershell -ExecutionPolicy Bypass -File scripts/multilingual-benchmark.ps1 -Mode Ocr

# 세 검사를 순서대로 실행
powershell -ExecutionPolicy Bypass -File scripts/multilingual-benchmark.ps1 -Mode All
```

보고서는 Git에 넣지 않는 `artifacts/multilingual-benchmark/`에 생성된다. 2026-08-13의 28개
기준 결과는 감지 66/66, Hy-MT2 1.8B 양방향 54건과 Discord 형식 1건 통과였다. 확장 과정에서
Hy-MT2 7B와 TranslateGemma 4B도 신규 8개 언어의 한국어 왕복 32방향을 통과했다. 발견한
필리핀어 영어 인사말 혼입, 우르두어 게임 문맥 어휘 혼동, 일부 언어의 문장 인칭 변화는 보수적
후처리 및 회귀 테스트로 고정했다. 이번 Windows GPU 환경의 warm 번역은 대체로 원문→한국어 약
0.07~0.14초, 한국어→대상 언어 약 0.05~0.26초였다. 이 검사는
문자 체계·보호 토큰·알려진 의미 회귀를 자동 판정하지만, 실제 서버 표본의 사람 평가는 계속
필요하다.
