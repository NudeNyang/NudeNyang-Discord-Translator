# 저장소 작업 규칙

이 문서는 NudeNyang Discord Translator 저장소에서 작업하는 모든 자동화 도구와 코딩 에이전트에 적용한다.

## 작업 시작

1. 작업 전에 `PROJECT.md`와 요청에 관련된 주제별 문서를 확인한다.
2. 현재 Git 상태를 확인하고 기존 변경을 사용자 작업으로 간주해 보존한다.
3. 요청 범위를 벗어난 기능이나 대규모 정리는 임의로 함께 수행하지 않는다.

## 구현 및 문서

- 앱 셸과 핵심 엔진은 Tauri/Rust를 기준으로 구현한다.
- Discord 연동은 사용자 토큰이나 비공식 Discord API에 의존하지 않는다.
- 제품에 표시되는 문구는 공식체로 작성한다. 사용자와의 대화 문체는 제품 문구 규칙과 분리한다.
- **툴팁은 항상 제품의 기존 커스텀 툴팁을 사용한다.** 앱·트레이·확장 등 제품 UI에서 HTML `title` 속성이나 브라우저 기본 툴팁을 새로 사용하지 않는다. 기존 `data-tooltip` 패턴과 디자인 토큰을 재사용하고, 마우스 호버와 키보드 `:focus-visible` 표시, 다국어 문구, 접근성 이름/설명을 함께 유지한다. 툴팁이 화면 경계나 부모 영역에 잘리지 않는지도 확인한다. 상세 기준은 `docs/BROWSER_EXTENSION.md`의 공통 툴팁 규칙을 따른다.
- 비개발자 사용자를 기준으로 설치, 인증, 오류 복구와 설정 경험을 설계한다.
- 중요한 설계 결정이나 제품 방향이 바뀌면 관련 Markdown 문서를 구현과 함께 수정한다.
- 제3자 모델이나 라이브러리를 추가할 때 라이선스와 배포 조건을 확인하고 고지를 갱신한다.

## 검증

- 버그 수정에는 가능한 한 재현 테스트를 먼저 추가한다.
- 완료를 보고하기 전에 변경 범위에 맞는 테스트, 포맷 검사와 빌드를 실제로 실행한다.
- 검증 실패를 무시하거나 성공으로 표현하지 않는다.
- 다른 작업의 미완성 변경 때문에 전체 검증이 불가능하면 해당 사실과 검증한 범위를 명확히 남긴다.

## 웹 번역 문제 해결 원칙

사용자가 특정 사이트의 번역 문제를 고쳐 달라고 요청했을 때:

1. 해당 사이트의 CSS selector, class, id, data-testid를 먼저 찾아 사이트별 예외를 추가하지 않는다.
2. 문제가 발생한 이유를 DOM 구조, HTML 의미, 가시성, 편집 가능 상태, 동적 렌더링 및 기존 수집·적용 로직에서 조사한다.
3. 사이트 도메인에 의존하지 않는 최소 HTML fixture로 문제를 재현한다.
4. 수정 전에 재현 테스트를 작성하고 현재 코드에서 실패함을 확인한다.
5. 같은 구조를 가진 다른 사이트에도 적용되는 범용 원인을 수정한다.
6. 프로젝트 코드를 변경한 뒤 전체 E2E 테스트를 실행한다.
7. E2E 테스트가 없거나 실행할 수 없다면 성공했다고 보고하지 않는다. 먼저 E2E 실행 환경을 만들거나, 막힌 이유를 명확히 보고한다.
8. 사이트별 예외가 정말 필요하다면 범용 해결이 불가능한 이유를 먼저 설명하고, 사용자 확인 없이 selector 예외를 추가하지 않는다.

기존 지원 범위와 사용자 보호 규칙은 회귀 테스트로 보존한다. 닉네임·핸들·URL·이메일 표시,
링크 목적지, 편집기·입력값, 코드, 메신저 동의 및 대화 경계를 리팩터링 때문에 훼손하지 않는다.
기존의 자연어 링크 문구 번역과 공개 메뉴·검색 라벨 번역도 유지한다. 개인정보 보호를 위한
서비스 범위와 단순 번역 누락을 보정하는 사이트 예외를 구분한다.

완료 보고에는 확인한 근본 원인, 수정 전 실패한 재현 테스트, 실행한 전체 E2E 명령과 결과를
포함한다. 모사한 번역 엔진·사이트 fixture를 사용하는 검증과 실제 로그인 사이트·본체 엔진
검증을 구분하며, 검사하지 않은 범위를 완벽 지원한다고 표현하지 않는다.

공통 DOM 수집·가시성·적용 경로를 수정할 때는 `docs/WEB_TRANSLATION_COVERAGE.md`의
독립 진단과 구조 생성 검사를 사용한다. 기대값은 제품의 수집 결과로 만들지 않는다.
전체 E2E와 별도로 `npm run test:public` 공개 표본 검사를 실행하고, 네트워크 차단이나
표본 변경으로 실행하지 못하면 이를 명시한다. 제외·화면 밖·검사 제한을 번역 성공으로
계산하지 않으며, 진단을 이유로 보호된 본문이나 개인 대화를 읽거나 외부에 내보내지 않는다.

## Git 커밋 필수 규칙

- **모든 작업은 완료 시 반드시 Git 커밋으로 남긴다.** 커밋하지 않은 상태로 작업 완료를 보고하지 않는다.
- 커밋 전 `git diff`와 `git status`를 확인하고 이번 작업에 포함할 변경만 스테이징한다.
- 하나의 커밋에는 하나의 설명 가능한 작업 단위를 담고, 커밋 메시지는 변경 목적을 분명하게 작성한다.
- 커밋 메시지는 한국어로 작성한다. 제품명, 기술명과 코드 식별자는 필요한 경우 원문 표기를 유지한다.
- 테스트 실패, 충돌 또는 외부 변경으로 안전한 커밋이 불가능하면 작업을 완료로 처리하지 않고 차단 사유를 보고한다.
- 빌드 산출물, 캐시, 로그, 인증 정보와 비밀 키는 커밋하지 않는다.
- 커밋은 로컬 저장소에 생성하되, 푸시·릴리스·배포는 사용자가 명시적으로 요청한 경우에만 수행한다.

## 작업 종료

1. 관련 테스트와 빌드 결과를 확인한다.
2. 문서 갱신 필요 여부를 다시 확인한다.
3. 변경 내역과 스테이징 범위를 검토한다.
4. 작업을 커밋한다.
5. 앱 동작, UI, 설정 또는 배포에 영향을 주는 작업이면 최신 릴리스 실행 파일을 생성하고 `NudeNyang Discord Translator (Tauri).lnk`의 대상, 작업 폴더와 아이콘을 해당 실행 파일로 다시 동기화한다.
   - Windows 패키징 또는 릴리스 작업에서는 `scripts/package_windows_variants.ps1`로 x64 설치형과 ARM64 설치형을 항상 함께 생성한다.
   - 자동 업데이트를 지원하지 않는 포터블판은 공개 릴리스에서 제외하며, GitHub 릴리스에는 두 설치형이 모두 준비된 것을 확인한 뒤 함께 게시한다.
   - 공개 Windows 업데이트는 `windows-x86_64`와 `windows-aarch64`를 모두 제공한다. 두 설치 파일·각 `.sig`·`latest.json` 플랫폼 항목·체크섬 중 하나라도 빠지거나 기존 공개 키로 서명 검증이 실패하면 배포를 중단한다. `scripts/package_github_release.ps1`과 `scripts/release-updates.mjs`의 검증을 우회하지 않는다.
   - GitHub 프리릴리스 여부와 앱 자동 업데이트 지원은 별개다. `-beta` 버전은 프리릴리스 및 `--latest=false`를 유지하며, ARM64 지원을 위해 정식 릴리스로 전환하거나 기존 업데이트 주소·서명 키를 교체하지 않는다.
   - 패키징은 로컬 업데이트 목록만 만든다. 두 아키텍처의 파일이 공개된 것을 확인한 뒤에만 `updates/beta/latest.json`을 커밋·푸시하여 기존 업데이트 주소에 반영한다.
6. 기존 NudeNyang Discord Translator 프로세스만 정확한 실행 파일 경로로 식별해 종료한 뒤, 동기화한 바로가기로 최신 실행 파일을 시작한다.
7. 실행 중인 프로세스의 경로와 제품 버전이 방금 빌드한 실행 파일 및 애플리케이션 버전과 일치하는지 확인한다.
8. 사용자에게 변경 내용, 검증 결과, 커밋 해시, 바로가기 동기화와 최신 실행본 실행 여부를 전달한다.

사용자가 명시적으로 빌드나 실행을 하지 말라고 요청한 경우에만 5~7단계를 생략한다. 그 외에는 작업 완료 보고 전에 항상 수행한다.
