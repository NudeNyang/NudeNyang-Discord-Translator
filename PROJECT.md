# Nude Translator 프로젝트 기준

## 제품 목표

Nude Translator는 Discord 데스크톱 클라이언트의 DOM을 이용해 받은 메시지, 보내는 메시지와
이미지의 텍스트를 번역하는 Windows 우선 데스크톱 애플리케이션이다. 앱 셸과 핵심 엔진은
Tauri/Rust를 기준으로 유지하며, 장기적으로 macOS를 지원할 수 있는 구조를 지향한다.

## 핵심 원칙

- Discord 사용자 토큰이나 비공식 Discord API를 사용하지 않는다.
- 채팅 텍스트와 이미지 OCR은 가능한 한 사용자 PC에서 처리한다.
- 외부 번역 서비스를 사용할 때는 전송 범위와 인증 방식을 명확히 표시한다.
- OCR은 부가 실험이 아니라 기본 제품 기능으로 유지한다.
- 제품에 노출되는 문구는 구어체를 사용하지 않고 공식체로 작성한다.
- 비개발자도 별도 콘솔 조작 없이 설치, 로그인, 모델 선택과 업데이트를 수행할 수 있어야 한다.
- Windows 구현이 다른 운영체제에 종속되지 않도록 플랫폼 경계를 분리한다.

## 현재 기술 방향

- 앱 셸 및 시스템 통합: Tauri 2, Rust
- 설정 UI 및 트레이 UI: HTML, CSS, JavaScript
- Discord 연동: Electron 전용 익명 파이프를 통한 CDP/DOM 제어
- 로컬 번역: Rust가 관리하는 `llama.cpp`와 GGUF 모델
- 기본 로컬 모델: Hy-MT2 1.8B
- 품질 우선 로컬 모델: Hy-MT2 7B
- 실험 로컬 모델: TranslateGemma 4B
- 이미지 번역: Rust OCR 파이프라인
- 설정 및 번역 기록: 사용자 로컬 저장소

## 문서 역할

- `README.md`: 사용자와 기여자를 위한 프로젝트 소개 및 실행 방법
- `ARCHITECTURE.md`: 런타임 구성, 경계와 데이터 흐름
- `HYMT.md`: 로컬 번역 모델, 프롬프트, 캐시와 품질 검증 기준
- `MACOS.md`: 향후 macOS 지원을 위한 제약과 준비 사항
- `TAURI_MIGRATION.md`: Tauri/Rust 전환 결정과 이행 기록
- `PRODUCT_LANGUAGE.md`: 제품 문구의 공식체 및 다국어 작성 규칙
- `THIRD_PARTY_NOTICES.md`: 모델과 외부 구성 요소의 라이선스 고지
- `AGENTS.md`: 저장소에서 작업하는 자동화 도구와 에이전트의 필수 규칙

설계 방향, 지원 범위, 모델 정책 또는 사용자 경험 원칙이 바뀌면 구현과 같은 작업에서 관련
문서를 함께 갱신한다.

## 완료 기준

작업은 다음 조건을 모두 충족해야 완료된 것으로 간주한다.

1. 요청한 구현 또는 문서 변경이 반영되어 있다.
2. 변경 범위에 맞는 테스트와 빌드 검증을 수행한다.
3. 실패한 검증이나 알려진 제한을 숨기지 않고 기록한다.
4. 의도하지 않은 파일이나 비밀 정보가 포함되지 않았는지 변경 내역을 확인한다.
5. 완료된 작업을 의미 있는 단위의 Git 커밋으로 남기고, 커밋 메시지는 한국어로 작성한다.

원격 저장소 푸시, 릴리스 발행과 배포는 사용자가 별도로 요청한 경우에만 수행한다.
